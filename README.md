# Basic flask template

## Steps to run

### Environment variables (WINDOWS)
```powershell
$env:FLASK_ENV = "development"
$env:FLASK_APP = "server.py"
```

### Use pipenv
```bash
pipenv install
pipenv shell
flask run
```
